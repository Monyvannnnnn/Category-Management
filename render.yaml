services:
  # ----------------------------------------------------
  # Web Application & Telegram Bot Webhook Service
  # ----------------------------------------------------
  - type: web
    name: inventory-web
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    region: singapore
    envVars:
      - key: PORT
        value: "80"
